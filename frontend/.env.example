# ── FreshEyes frontend — environment variables ─────────────────────────
# Copy this file to `.env.local`.

# Base URL of the FreshEyes backend (Express SSE server).
# Optional for local dev (defaults to http://localhost:8787); REQUIRED in
# production — point it at your deployed backend URL.
NEXT_PUBLIC_API_BASE=http://localhost:8787
